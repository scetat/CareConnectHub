{
  "name": "care connect hub",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "frontend": "cd frontend && npm start",        
    "backend": "cd backend && node server.js",  
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""  
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    
  }
}

